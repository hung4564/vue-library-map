import { ListCompareView, ListOption } from "@/interface/datasource/list";
import { Layer } from "../..";
export declare function createDefaultCompareViewInList(
  layer: Layer,
  option?: ListOption,
  maps?: string[]
): ListCompareView;
